var arr = [];
var count = 0;
var alpha = 'asdАБСABC';
var elem = 0;
do {
    elem = prompt('Введите число', '');
    count++;
    arr.push(elem);
} while (elem != '');

if (elem == '') {
    for (var i = 0; i < count; i++) {

    }
}
